# Gaps

# i3 gaps
smart_gaps on
hide_edge_borders none

for_window [workspace=$work_office] gaps inner current set 10; gaps outer current set 0

# quick gaps configurations
bindsym $Alt+Shift+equal gaps inner current set 0; gaps outer current set 0
bindsym $Alt+equal gaps inner current set 10; gaps outer current set 0

# resizing the GAPS for all workspaces
bindsym $Super+Shift+equal gaps inner all set 0; gaps outer all set 0
bindsym $Super+equal gaps inner all set 10; gaps outer all set 0

