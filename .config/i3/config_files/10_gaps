# Gaps

# i3 gaps
smart_gaps on
hide_edge_borders both
for_window[class="^.*"] border pixel 0

for_window[workspace=$work_office] gaps inner current set 10; gaps outer current set 0

# quick gaps configurations
bindsym $Alt+Shift+plus gaps inner current set 0; gaps outer current set 0
bindsym $Alt+plus gaps inner current set 10; gaps outer current set 0

# resizing the GAPS for all workspaces
bindsym $Super+Shift+plus gaps inner all set 0; gaps outer all set 0
bindsym $Super+plus gaps inner all set 10; gaps outer all set 0

